# Project log: LangExtract docs site

The single persistent record of this project's process across every agent and session. If you are an agent working on this project: READ THIS FILE FIRST to load the full picture, and UPDATE IT LAST before you finish (refresh Current state, add a Log entry). This is what keeps continuity when the work spans different agents at different times.

## Current state
(Overwrite this section each session. The at-a-glance picture of where things stand right now.)
- **Project directory:** `langextract-docs` (inside `C:\Users\jayro\Documents\GitHub`). This is the project root. (Note: the original briefing pointed at `...\langextract`; the actual location is `...\langextract-docs`.)
- **Fork:** `wordcaster/langextract` (fork of `google/langextract`), cloned to the project dir. Working branch: `docs-site`.
- **Upstream pinned to:** tag **v1.5.0**, commit `116c0aace3c6e4cda49308324e58c1d171fab0cb`. The docs site is committed on branch `docs-site` and pushed to `origin/docs-site`; `main` remains a clean mirror of upstream.
- **Phase / step:** Session 6 — **docs expanded, then restyled to the Google developer documentation style guide; committed and pushed to `docs-site`.** The site went from the four-page slice to a Diátaxis-clean, usable site: Introduction → Quickstart → Concepts (how-extraction-works, grounding, model-backends) → How-to guides (long-document-workflow, use-openai, use-ollama, write-prompts-and-examples, work-with-results, api-keys) → Reference (api). All three task phases done. Awaiting John's stranger-read of the running site + the deploy decision; chat-Claude runs the accuracy/Diátaxis/cross-ref audits.
- **Em-dash removal (Session 7, 2026-06-22):** Removed every em dash from the published-page prose: 98 across the 12 pages, each replaced with a colon, comma, or parentheses by role. The 5 em dashes inside code blocks were preserved. Gates all pass: code-block diff guard 0 of 42 changed; em-dash census 0 outside code (was 98); build green with the three throw gates. Future pages should avoid em dashes so this does not recur.
- **Docs structure now:** 12 pages, one Diátaxis type each. The admonition bracket-syntax bug is fixed (intro/quickstart/long-document-workflow). Two old concept pages (prompts-and-examples, working-with-results) were rehomed into How-to and deleted. The sidebar is autogenerated in journey order. Build gate is strict: `onBrokenLinks`, `onBrokenAnchors`, and `onBrokenMarkdownLinks` all `throw`, and the build is green.
- **Example harness (this session):** the docs' Gemini code examples are now executable-tested. An out-of-repo harness ran every page's `python` blocks: **4 execute pages passed (16 blocks) live against Gemini** (quickstart 3 extractions, long-document 10 grounded, work-with-results 2, write-prompts object-construction), **5 blocks syntax-checked** (use-openai/use-ollama/api-keys — no reachable backend), and `reference/api.md` (12 pseudo-signature blocks) skipped as illustrative. Key loaded from the local `.env` for the run only — never printed, written, or committed; harness + venv lived outside the repo and were removed.
- **Google style pass (Session 6):** restyled prose across all 12 pages to the Google developer documentation style guide (second person, present tense, active voice; removed minimizers/idioms; parallel imperative lists; sentence-case headings; descriptive links). **No code changed** — a fenced-code-block diff guard confirmed **0 of 42** blocks differ. Build green with `onBrokenLinks` + `onBrokenAnchors` + `onBrokenMarkdownLinks` all `throw`; admonition scan clean (0 malformed opens, 0 literal `:::` in rendered HTML).
- **Live quickstart check:** ✅ **PASSED (2026-06-21).** Ran the smoke test against the live Gemini API with `model_id="gemini-3.5-flash"` (key supplied from a local `.env` outside the repo, in a throwaway venv pinned to langextract 1.5.0). Result: `SMOKE_PASS` — 2 grounded extractions returned (`person=Tom`, `mood=nervous`). The documented quickstart model is confirmed callable end-to-end. Key was never printed, written, or committed; the temp venv lived outside the repo and was removed afterward.
- **What builds:** `npm run build` succeeds with **no errors and no warnings**; no broken links (`onBrokenLinks: 'throw'`). Verified on the *served* build: all 7 slice routes return 200, removed routes (`/blog`, `/markdown-page`) 404, homepage carries the new content with zero template strings remaining. `node_modules` already installed.
- **Phase 2 done this session (gaps closed):**
  - `docusaurus.config.js` fully rewritten — real title/tagline, GitHub-Pages URL targets (`wordcaster.github.io/langextract`, not deployed), navbar (Docs + API reference + GitHub→**upstream google/langextract**), 3-column footer, and the **required "not an officially supported Google product" disclaimer** in the footer copyright + on the homepage. `editUrl` points at the fork's docs branch.
  - Homepage rewritten (`src/pages/index.js` + `HomepageFeatures`) — real LangExtract hero, CTAs to Quickstart/Introduction, six real feature cards, disclaimer. No template content remains.
  - Original logo + favicon (`static/img/logo.svg`, also used as favicon); Docusaurus dino assets removed.
  - Demo blog, `markdown-page.mdx`, and all unused template images deleted; their nav/footer links removed.
  - Sidebar kept **autogenerated** (John's call) — clean, journey-ordered subset of real pages only.
  - `CONTENT-MODEL.md` promoted to a non-draft at the project root, matching the built structure.
  - Git identity reconciled to **John Rojas / jay.rojas@gmail.com**; first commit made and pushed to `docs-site`.
- **Content — DONE and high quality (all source-grounded):**
  - `docs/intro.md` — overview, real, carries the "not an official Google product" disclaimer.
  - `docs/quickstart.md` — install → first extraction → visualize, end to end.
  - `docs/concepts/` — THREE finished concept pages (how-extraction-works, prompts-and-examples, working-with-results). Exceeds the one-page minimum.
  - `docs/reference/api.md` — source-grounded API reference. The "pending verification" admonition was **removed in Session 3** per John's accuracy sign-off; the page keeps the honest version pin ("drawn from the LangExtract source at version 1.5.0"). Load-bearing defaults verified against `langextract/extraction.py`: `model_id="gemini-3.5-flash"`, `max_char_buffer=1000`, `batch_length=10`, `max_workers=10`, `extraction_passes=1`, `min(batch_length, max_workers)`.
  - Content model (`source-material/content-model-DRAFT.md`) and IA (`source-material/ia-DRAFT.md`) drafts exist and match the built structure.
- **Next (later passes, NOT in this slice):** John reviews the running site (prose/IA quality, concept selection, API coverage — his calls); expand the API reference to the deferred surface (per-provider kwargs, tokenizer, resolver, chunking, Annotator); add How-to recipes (OpenAI / Ollama / long docs / custom provider) and a Troubleshooting page; decide deploy (GitHub Pages default `wordcaster.github.io/langextract`) — **not yet authorized.**
- **Awaiting John (review gates — do NOT self-certify):**
  1. Review the running site: does the prose read well and is the IA genuinely good? (`cd docs-site && npm run start`)
  2. Accuracy/completeness sign-off on the API reference (drafted from source; defaults spot-checked vs source AND the quickstart model `gemini-3.5-flash` now confirmed callable live — a full human pass over the remaining surface is still your call).
  3. Whether the concept selection and API coverage are the right calls.
  4. Authorization for anything *further* public: deploying, or opening a PR upstream. (First commit was pushed to the `docs-site` branch this session, per the approved task list. No deploy, no upstream PR.)
  5. Still-open from the Session-1 drafts, not blocking the slice: whether to add an "About / who built this" page; 4-type vs 2-bucket model (kept 4-type).

## Log (append-only, newest last; one entry per working session)
Format: date | agent | what was done | decisions made and why | handed off / left for next | open questions
- 2026-06-15 | Claude Code (Opus 4.8) | **Session 1 — scaffold + first-pass preview.** Forked `google/langextract` to `wordcaster/langextract`; cloned into `langextract-docs`; created branch `docs-site` from tag **v1.5.0** (commit `116c0aa`) to pin everything to the latest released version users `pip install`. Created PROJECT-LOG.md + build-log.md. Harvested `source-material/` (content-inventory.md, api-inventory.md) by reading the package source directly. Scaffolded Docusaurus 3.10.1 classic-js in `docs-site/` (baseline build green). Drafted content model + IA previews. Drafted first-pass pages: quickstart, 3 concept pages (how-it-works, prompts-and-examples, working-with-results), and a source-grounded API reference. Rebuilt clean. | **Key decisions (each with the alternative I weighed):** (1) **Pin to v1.5.0 tag, not `main`** — documents what `pip install langextract` gives users; alt was tracking `main` (rejected: moving target, would drift from released behavior). (2) **classic-js over classic-ts** — matches the DOCUSAURUS_VARIANT decision and lowers contributor friction; alt was TS (more type safety, more ceremony). (3) **Content model = 4 types: Tutorial / Concept / How-to / Reference** (Diátaxis-aligned) — see content-model-DRAFT.md; alt was a flat "Guides + API" split (rejected: blurs the learning-vs-reference boundary that trips up new users). (4) **IA developer journey: Understand → Start → Build → Reference → Troubleshoot** — see ia-DRAFT.md; alt was provider-first IA (rejected: most users start with Gemini default and don't pick a provider until later). (5) **API reference grouped by task (extract / data types / model config / providers / visualization & IO / exceptions)** rather than alphabetical module dump — better for findability; alt was autodoc-style module listing. | **Left for next:** John reviews in this order — content model → IA → pages → API accuracy. Then redirect. Expansion candidates noted under "Next". | **Open questions:** model_id default in v1.5.0 source is `gemini-3.5-flash` (verified in `extraction.py:40`); confirm that's the intended documented default. Deploy target undecided. Whether to keep the default Docusaurus blog (currently left in place, unlinked emphasis) — flagged in ia-DRAFT.md.

- 2026-06-21 | Claude Code (Opus 4.8) | **Session 2 — Phase 1 orientation (no building).** Located the build at `langextract-docs` (briefing pointed at `langextract`). Confirmed branch `docs-site` sits on upstream v1.5.0 with all docs work untracked. Surveyed every page: intro, quickstart, 3 concept pages, and the API reference are real, finished, high-quality content — no stubs or lorem ipsum in the docs themselves. Verified `npm run build` is green (no broken links). Spot-checked the API reference's load-bearing defaults against `langextract/extraction.py` — all correct. | **Key finding:** the docs *content* is essentially Phase-2-complete and good; the gaps are entirely in the **un-customized Docusaurus scaffold** — config (title/tagline/url/org/nav/footer/editUrl all vanilla), the default homepage splash, leftover demo blog + markdown-page + template assets, and a missing footer disclaimer. Corrected the prior log's inaccurate claim that the homepage/footer already reflected the disclaimer (they don't). | **Left for next:** STOPPED per Phase 1 rules; reported gap list + proposed Phase 2 task list to John; awaiting his go-ahead and his calls on the open IA/content-model questions. | **Open questions:** unchanged from Session 1 (content model 4-type vs 2-bucket; blog fate; GitHub link target; sidebar autogen vs hand-authored; About page) plus: reconcile git identity to `John Rojas` / `jay.rojas@gmail.com` before the first commit?
- 2026-06-21 | Claude Code (Opus 4.8) | **Session 2 (cont.) — Phase 2: built the complete first slice.** John approved the proposed task list and gave four calls: proceed (with commit), delete the demo blog, GitHub link → upstream `google/langextract`, keep the autogenerated sidebar. Executed all gap-closing work: rewrote `docusaurus.config.js` end-to-end (real metadata, GitHub-Pages URL targets, navbar Docs+API+GitHub, 3-column footer, the required Google disclaimer in copyright); rewrote the homepage (`index.js` hero + CTAs, six real `HomepageFeatures` cards, disclaimer); replaced the dino logo/favicon with an original SVG mark; deleted `blog/`, `markdown-page.mdx`, and unused template images and their links; set `blog: false`; migrated the deprecated `onBrokenMarkdownLinks` under `markdown.hooks`. Promoted `CONTENT-MODEL.md` to a non-draft at project root. Reconciled git identity to John Rojas / jay.rojas@gmail.com. | **Decisions:** kept `baseUrl: '/langextract/'` + `url: wordcaster.github.io` to pre-wire the stated GitHub-Pages deploy default without deploying; used the logo SVG as the favicon to avoid shipping a binary `.ico`; six homepage feature cards (two clean rows) over three; left the green Infima palette (professional, on-brand-enough). | **Verification:** clean `npm run build` (no errors, no warnings); on the served build all 7 slice routes 200, `/blog` + `/markdown-page` 404, homepage carries new content with zero template strings, nav/footer/editUrl targets correct. Committed and pushed to `docs-site`. | **Left for next:** John's review of the running site (prose/IA/coverage are his calls); later passes = deeper API reference + How-to/Troubleshooting pages; deploy is a separate, still-unauthorized step. | **Open questions:** API reference still needs full human accuracy verification; deploy target/timing; whether to add an About page.
- 2026-06-21 | Claude Code (Opus 4.8) | **Session 3 — two small follow-up edits + conditional live check.** Per John's direction: (1) removed the "Draft — pending verification" admonition from `docs/reference/api.md` (John's accuracy sign-off against v1.5.0; the honest version-pin sentence stays); paragraphs before/after left intact. (2) Tidied `docs-site/sidebars.js` — removed the leftover Docusaurus template comments and the commented-out manual-sidebar example, keeping the single autogenerated `tutorialSidebar`. | **Live quickstart check:** **Skipped (no `LANGEXTRACT_API_KEY` in environment); run manually.** Verified key absence in both Git Bash and PowerShell (process + User + Machine scopes) without ever reading/printing the value; no key was prompted for, written, or committed. The documented quickstart model `gemini-3.5-flash` is therefore NOT yet confirmed callable — left for a manual run when a key is present. | **Verification:** `api.md` no longer contains "pending verification"; `sidebars.js` no longer contains the template comments; `npm run build` clean (no errors/warnings). Committed and pushed to `docs-site`; `main` untouched; no temp/live-check files staged. | **Open questions:** still need a live confirmation that `gemini-3.5-flash` resolves and returns extractions before any deploy — run the smoke check with a key set.
- 2026-06-21 | Claude Code (Opus 4.8) | **Session 4 — ran the deferred live quickstart check (now unblocked by a key).** John pointed to a local `.env` (in a different repo, outside this one) holding `LANGEXTRACT_API_KEY`. Built a throwaway venv in `%TEMP%\le-smoke` (outside any repo), `pip install langextract==1.5.0`, wrote/ran the smoke script with `model_id="gemini-3.5-flash"`. | **Result: `SMOKE_PASS`** — 2 grounded extractions (`person=Tom`, `mood=nervous`). The documented quickstart model is confirmed callable end-to-end, which closes the open risk flagged in Session 3. | **Key safety:** loaded the value into the env only for the single run, stripping CR/quotes/whitespace; never printed, written, or committed it; piped output through a redaction filter as defense-in-depth; removed the temp venv afterward. Only `PROJECT-LOG.md` changed in the repo (no smoke/venv files staged). | **Open questions:** none new — remaining items are John's review of the running site + the deploy decision.
- 2026-06-21 | Claude Code (Opus 4.8) | **Session 5 — expanded the docs to a usable, Diátaxis-clean site (3-phase task).** **Phase 1 (restructure, reviewed by John):** fixed the admonition bracket-syntax bug (intro/quickstart/long-document-workflow); restructured IA to Introduction → Quickstart → Concepts (how-extraction-works trimmed; grounding + model-backends new) → How-to guides (long-document-workflow, use-openai, use-ollama, write-prompts-and-examples, work-with-results, api-keys) → Reference; deleted the two old concept pages after rehoming; repointed footer + api.md links. **Phase 2 (content):** wrote the flagship long-document workflow (chunking/parallelism/passes levers in one runnable scenario), the OpenAI / Ollama / API-keys how-tos, and lifted grounding/results prose into their new homes — all grounded in v1.5.0 source (harvested factory.py/openai.py/ollama.py/extraction.py/pyproject.toml). **Phase 3 (scans):** strict build gate (onBrokenLinks + onBrokenAnchors + onBrokenMarkdownLinks all `throw`) green; executable example harness — 4 execute pages passed live on Gemini (16 blocks: quickstart 3, long-document 10 grounded, work-with-results 2, write-prompts object-construction), 5 blocks syntax-checked (OpenAI/Ollama/api-keys), api.md (12 signature blocks) skipped as illustrative, 0 failures; MDX scan — 0 literal `:::` in rendered HTML, all admonitions bracket-titled and rendered, all fences balanced; terminology — no banned words, consistent LangExtract casing. | **Decisions / corrections:** adversarial review caught a real accuracy blocker — the OLLAMA_BASE_URL claim in use-ollama.md was wrong (the env var is only read when `"ollama"` is a substring of `model_id`, so it doesn't apply to `gemma2:2b`); fixed to "pass `model_url` for a non-default server." Dropped the brief's "export CSV" task per John — v1.5.0 has CSV *input* only, no export (would be invented API). Changed a `→` to `->` in a work-with-results print so the example is copy-paste-safe on Windows cp1252 consoles. Long-document flagship uses a short multi-paragraph sample (no truncation needed) so its runnable example is cheap and self-contained. | **Key safety:** the harness key came from the local `.env` (outside this repo), loaded for the run only with CRLF/quotes stripped, never printed/written/committed, redacted from captured output; harness + venv outside the repo and removed. | **Left for next:** John's stranger-read + deploy decision; chat-Claude's accuracy/Diátaxis/cross-ref audits. Later passes: a custom-provider how-to, a Troubleshooting page, deeper per-area reference. | **Open questions:** deploy target/timing (GitHub Pages default pre-wired, not run); whether to add a Troubleshooting page next.
- 2026-06-21 | Claude Code (Opus 4.8) | **Session 6 — Google developer documentation style-guide pass (prose only).** Restyled prose across all 12 pages to developers.google.com/style: passive → active voice (notably the pipeline-stage and alignment descriptions, now "LangExtract splits / sends / parses / locates / collects…"), present tense ("catches" not "will catch"), second person, removed a minimizer ("easy") and idioms ("out of the box", "kicks in"), made the example-writing rules a parallel imperative list, and tightened a few vague "this" subjects and "are listed in" → "see" handoffs. | **Hard guardrail held — prose only.** A fenced-code-block diff guard (extract every ``` block before vs after, then `diff`) confirmed **0 of 42 code blocks changed**; no parameter, value, signature, model id, or path was touched, so the Session-5 example harness stays valid. | **Verification:** `npm run build` green with `onBrokenLinks` + `onBrokenAnchors` + `onBrokenMarkdownLinks` all `throw` (no links/anchors broken by edited link text); admonition scan — 0 malformed space-title opens in source and 0 literal `:::` in rendered HTML; word-list sweep — no "simply / just / easily / obviously / in order to / e.g. / i.e. / allows-or-enables you to / please / whitelist / blacklist" surviving in prose. Added a style-guide line to `CONTENT-MODEL.md`. | **Left for next:** chat-Claude's accuracy / Diátaxis / cross-reference audits; John's stranger-read + deploy decision. | **Open questions:** none new.
- 2026-06-22 | Claude Code (Opus 4.8) | **Session 7: em-dash removal from page prose (prose only, punctuation swap).** Removed every em dash (U+2014) from the prose of the 12 published pages and replaced each with cleaner punctuation by role: bulleted `**term** —` openers and see-also links became colons; bracketed asides became parentheses; clause-joining dashes became sentence splits or commas. The one standalone default-cell em dash in the api.md parameter table (`text_or_documents`, a required positional) became `(required)`. | **Gates (all pass):** (1) code-block diff guard: 0 of 42 fenced blocks changed (re-extract plus `diff` against a pre-pass snapshot), so no code, parameter, value, signature, or path was touched; (2) em-dash census outside code blocks: 98 before, 0 after (per-file before: api 26, grounding 12, write-prompts 12, long-document 9, intro 8, how-extraction-works 7, model-backends 6, quickstart 5, api-keys 4, use-openai 4, work-with-results 3, use-ollama 2); (3) collateral: en dashes still 0, arrows still 2, hyphens untouched; the 5 em dashes inside api.md code comments (lines 28, 166, 212, 296, 298) preserved, so the total em-dash count is now 5; (4) `npm run build` green with onBrokenLinks, onBrokenAnchors, and onBrokenMarkdownLinks all `throw`. The 7 reference heading slugs that changed (for example `## 3. Model configuration: lx.factory`) broke no anchors, since only `#1-lxextract`, `#2-data-types`, and `#4-providers--model-routing` are referenced and none of those changed. | **Scope:** repo-meta files (PROJECT-LOG, build-log, README, CONTENT-MODEL) left out of the transform. Snapshot and census scripts lived outside the repo and were not committed. | **Left for next:** John reviews the colon/comma/parentheses reading choices on the running site (deferred, not self-certified). | **Open questions:** none new.

## Artifact map (where the pieces live)
- **PROJECT-LOG.md** (this file): cross-agent process state and continuity. Start and end here.
- **Repo root:** a fork of `google/langextract` (`wordcaster/langextract`), cloned inside `C:\Users\jayro\Documents\GitHub\langextract-docs`, branch `docs-site` @ v1.5.0.
- **build-log.md:** granular build decisions and friction; raw material for the loop article. Keep technical; strategic/process decisions go in this PROJECT-LOG.
- **source-material/:** harvested LangExtract raw material (read-only reference; do NOT ship in the site).
  - `content-inventory.md` — every existing doc section in the upstream repo, one line each.
  - `api-inventory.md` — the public API surface read from package source (signatures, types, defaults).
  - `content-model-DRAFT.md` — first-pass content model preview (superseded by `CONTENT-MODEL.md` at the project root; kept as history).
  - `ia-DRAFT.md` — first-pass IA / nav / sidebar / developer-journey preview (decisions + alternatives).
- **CONTENT-MODEL.md** (project root): the approved, non-draft content model the site is built on. Reflects the actual file tree + autogen sidebar.
- **docs-site/:** the Docusaurus site (classic-js, Docusaurus 3.10.1). Scaffold chrome (config, homepage, branding) is now real, not template.
  - Pages live in `docs-site/docs/` — content awaiting John's review of the *running* site.
- **career-plan.md / docusaurus-build-and-article-plan.md:** referenced by the original brief as strategic artifacts; NOT created this session (out of scope for the scaffold/preview pass). Create when John brings that material.

## Notes for the next agent
- The content in `docs-site/docs/` is built and pending John's review of the running site — treat as draft-until-approved; do not deploy without his go-ahead.
- Site content **is now committed and pushed to the `docs-site` branch** of the fork (John approved the first commit). Still **NO deploy and NO upstream PR** — keep it that way until John says otherwise. `main` remains a clean mirror of upstream v1.5.0.
- LangExtract is **not an officially supported Google product** (per its own README "Disclaimer"). Never frame this site as official Google documentation. As of Phase 2 the homepage AND footer carry this disclaimer (verified rendered) — earlier logs that claimed this before Phase 2 were aspirational.
- Fixed facts: author is **John Edgar Rojas**; experience is **17 years**.
- To re-verify any API claim: the pinned source is in `langextract/` at branch `docs-site` (v1.5.0). Cite `file:line`.
- **Maintenance — examples are tested, so they should not silently rot.** The Gemini code examples in the docs are executed against the live API by an out-of-repo harness (Session 5). When you change a code example or bump the documented LangExtract version, re-run that harness (it needs `LANGEXTRACT_API_KEY`) so a broken example is caught rather than shipped. The OpenAI/Ollama/credential examples are syntax-checked and source-verified instead (no reachable backend in CI).
- **Maintenance — the content model is built to grow without restructuring.** Each page is exactly one Diátaxis type (Tutorial / Concept / How-to / Reference) and the sidebar is autogenerated from the folder layout + `sidebar_position`. A new page slots into an existing type (drop it in `concepts/`, `how-to/`, or `reference/` with a position) — no IA rework. Do not reintroduce type-mixing (no task steps in concept pages; no reference field-tables duplicated outside `reference/api.md`).
- **Maintenance: no em dashes in page prose (Session 7).** The published pages under `docs-site/docs/` use no em dashes (U+2014); each was replaced with a colon, comma, or parentheses by role. New or edited pages must avoid em dashes so this one-time cleanup does not recur. Em dashes inside fenced code blocks are fine and were deliberately preserved; the diff guard that proves a prose pass touched no code is `extract every fenced block before and after, then diff`.
