# Build log: LangExtract docs site

Append one line per decision or friction, in real time. Raw material for the Phase 2 "document any tool from scratch" loop article. Capture the WHY, not just the what.

Context:
- Tool: LangExtract (google/langextract)
- Upstream version read: **v1.5.0** (tag), commit `116c0aace3c6e4cda49308324e58c1d171fab0cb`
- Repo mode: fork (`wordcaster/langextract`)
- Project dir: `langextract-docs` (inside `C:\Users\jayro\Documents\GitHub`)
- Site dir: `docs-site`
- Toolchain: node v24.13.0, npm 11.6.2, Docusaurus 3.10.1 (classic-js), React 19
- Start date: 2026-06-14

## Log (date | step | decision or friction | how resolved | reusable in the loop?)
- 2026-06-15 | preconditions | Checked node/npm/git/gh up front before touching anything. | All present; `gh` authed as `wordcaster` with `repo`+`workflow` scopes. github.com reachable. | YES — a 60-second precondition gate saves a half-built repo later. Always verify auth scope, not just "is gh installed."
- 2026-06-15 | version pinning | Decided what "the version" even means: `main` HEAD vs latest release. | Pinned to tag **v1.5.0** by creating branch `docs-site` from the tag. Documenting the released version users actually install beats chasing `main`. | YES — "which commit are you documenting?" is the first real decision of any docs-from-source job. Pin it and write it down before harvesting.
- 2026-06-15 | harvest | `langextract.data` and `langextract.exceptions` are *shims* — they re-export from `langextract.core.*`. Reading the top-level file alone would have missed every real definition. | Followed the re-export to `core/data.py`, `core/exceptions.py`, `core/types.py` for the actual dataclasses/enums. | YES — public import path != where the code lives. Always chase `from x import *` / re-exports to the source of truth before writing a reference.
- 2026-06-15 | harvest | The package uses PEP 562 lazy submodule loading (`__getattr__` in `__init__.py`). The public surface is the `__all__` list + lazy modules, not what's eagerly imported. | Took the public API contract from `__all__` (extract, visualize, + lazily-exposed submodules data/io/factory/providers/exceptions/...). | YES — `__all__` is the contract; lazy loading hides it from naive `dir()` inspection.
- 2026-06-15 | harvest | `lx.extract()` has 30+ parameters, several deprecated (`language_model_type`, `gemini_schema`) with v2.0.0 removal warnings. | Captured the full signature from `extraction.py` incl. defaults and the keyword-only block; flagged deprecations explicitly so the reference can steer users to `model`/`config`/`model_id`. | YES — surface deprecations in the reference, don't silently document dead params. Source docstrings already say "removed in v2.0.0" — quote them.
- 2026-06-15 | harvest | Model-ID → provider routing is regex-pattern based (`providers/patterns.py`), not a hardcoded list. e.g. `^gemini` → Gemini, `^gpt-4|^gpt-5|...` → OpenAI, a long Ollama list, all priority 10. | Documented the actual patterns so users can predict which provider a model_id resolves to (and why a custom id needs explicit `config`). | YES — for any plugin/registry system, document the *matching rule*, not just the examples that happen to work.
- 2026-06-15 | scaffold | `create-docusaurus` can prompt for package manager and hang a non-interactive run. | Ran with `--package-manager npm --javascript` and `CI=true` to force non-interactive; piped output to background task. Exit 0. | YES — always pass package-manager + variant flags to scaffolders; never assume the prompt won't block an agent.
- 2026-06-15 | scaffold | Docusaurus 3.10.1 `package.json` declares `engines.node >=20`, while the brief's floor was node >=18. | We're on node 24, so moot — but noted: the *scaffold's* node floor (20) is stricter than LangExtract's or the brief's. | YES — the docs toolchain has its own runtime floor independent of the tool being documented. Check both.
- 2026-06-15 | scaffold | Verified clean baseline `npm run build` BEFORE adding any content. | Green (generated static files). Establishes that any later build break is *my* content, not the scaffold. | YES — bank a known-good baseline build before editing. Cheap bisection insurance.
- 2026-06-15 | content | Where to put drafts so a build break in one page doesn't sink the site, and so John reviews intentionally. | Put all first-pass pages under `docs-site/docs/`; kept inventories + model/IA drafts in `source-material/` (never shipped). Build after each batch. | YES — keep "thinking" artifacts (inventories, model/IA drafts) out of the shippable docs tree; reviewers shouldn't have to dig past your scaffolding.
- 2026-06-15 | content | FRICTION: set `slug: /` on the intro doc to make it the docs landing, but the scaffold's homepage button AND footer both hardlink to `/docs/intro`. With Docusaurus default `onBrokenLinks: 'throw'`, that would have failed the build. | Removed the slug override; intro stays at `/docs/intro` so the existing scaffold links resolve. Deferred any navbar/footer/branding rewrite to a post-approval pass (flagged in ia-DRAFT.md). | YES — changing a doc's slug silently breaks every hardcoded link the scaffold ships with. Either grep the scaffold for the old path first, or don't move the landing doc until you own the config.
- 2026-06-15 | content | Cross-page links: used slug-relative links (`quickstart`, `../reference/api#anchor`) rather than `.md`-relative. | Verified by full `npm run build` (broken-link checker is on by default) — all resolved, exit 0. Also probed the running server: all 6 routes return HTTP 200. | YES — trust but verify: Docusaurus' throw-on-broken-link build IS your link test. Build after writing links; don't eyeball them.
- 2026-06-15 | decision | Deliberately did NOT touch `docusaurus.config.js` (title still "My Site", tagline "Dinosaurs are cool", navbar "Tutorial", demo footer links). | Branding/nav commits to IA decisions that are John's call. Left at scaffold defaults and flagged; the disclaimer ("not an official Google product") is carried in the intro page content instead, where it's safe to assert. | YES — separate "make it build + draft the content" from "commit to the site's identity." Don't burn review trust by quietly deciding branding the conductor hasn't approved.
- 2026-06-15 | scope | Did NOT `git commit`. | Brief says work locally; house rule says commit only when asked. Left changes in the working tree on branch `docs-site` so John reviews a clean diff and decides when/how to commit. Nothing pushed, no deploy, no upstream PR. | YES — for a review-gated handoff, an uncommitted working tree IS the deliverable; let the conductor own the commit boundary.

- 2026-06-21 | orient | Picked up a build whose docs were done but whose scaffold was untouched. Read PROJECT-LOG + both IA/content drafts before changing anything; spot-checked the API reference's defaults against `extraction.py` rather than trusting the prior session's self-report. | All defaults verified (`gemini-3.5-flash`, 1000, 10/10, passes=1). Confirmed the *content* was finished and the gap was entirely scaffold chrome. | YES — when inheriting work, verify the load-bearing claims against source yourself; a prior agent's "done" is a hypothesis, not a fact.
- 2026-06-21 | config | The scaffold's `docusaurus.config.js` ships ~12 distinct facebook/docusaurus placeholders (title, tagline, url, org, project, navbar, footer x3 columns, editUrl, copyright, social image). Each is invisible until you read the file — the *site builds green* with all of them wrong. | Rewrote the whole config in one pass: real title/tagline, GitHub-Pages URL targets, navbar (Docs + API reference + GitHub→upstream), footer (Docs/Concepts/Project columns), and the required "not an official Google product" disclaimer in the copyright. | YES — a green build proves links resolve, NOT that the chrome is real. "Builds clean" and "is finished" are different acceptance tests; the template's defaults all pass the first and fail the second.
- 2026-06-21 | config | Deleting the demo blog is two coupled edits, not one: removing `blog/` AND setting `blog: false` in the preset AND stripping every `/blog` navbar/footer link. With `onBrokenLinks: 'throw'`, leaving a stray `/blog` link would have failed the build. | Did all three together; rebuilt; `/blog` and `/markdown-page` now correctly 404. | YES — removing a scaffold feature means chasing every link the scaffold wired to it; the broken-link checker is the safety net that catches a missed one.
- 2026-06-21 | config | `onBrokenMarkdownLinks` at the config top level is deprecated under `future.v4: true` and warns on every build. | Moved it to `markdown.hooks.onBrokenMarkdownLinks`. Clean build, no warnings. | YES — if you opt into `future.v4`, also migrate the options v4 relocates, or you eat a warning every build.
- 2026-06-21 | branding | The scaffold's `logo.svg` and `favicon.ico` are the Docusaurus dino. | Replaced with a small original SVG mark (brackets + a highlighted span = "extract a grounded span") and pointed both the navbar logo and favicon at it, dropping the binary favicon. | YES — an SVG favicon (`favicon: 'img/logo.svg'`) avoids generating a binary `.ico` and keeps one source of truth for the mark.
- 2026-06-21 | homepage | The default `index.js` splash ("Docusaurus Tutorial - 5min ⏱️") and `HomepageFeatures` ("Easy to Use / Powered by React" with undraw SVGs) are the most visible lorem-ipsum on the whole site, and the only ones a first-time visitor sees. | Rewrote the hero (real CTAs to Quickstart + Introduction), replaced the three template cards with six real LangExtract feature cards (no illustration dependency), and added the disclaimer below the fold. | YES — the homepage is outside the `docs/` tree, so a "docs are done" pass misses it entirely. Audit `src/pages/` and `src/components/` separately from `docs/`.
- 2026-06-21 | verify | Confirmed the slice end-to-end on the *served build*, not just a green compile: curled all 7 routes (200), the two removed routes (404), grepped the homepage for new strings present + every template string absent, and checked nav/footer/editUrl targets. | All green. | YES — "200 on every real route + 404 on every deleted route + zero template strings in the HTML" is a fast, scriptable acceptance gate that a build success alone doesn't give you.

What to capture: why you chose the content types you did, where the IA fought you, a Docusaurus config gotcha, a place a developer would get lost, where John redirected you and what you drafted well or poorly, how the direct-and-review loop felt, anything you would tell the next person conducting a doc-site build from scratch.
